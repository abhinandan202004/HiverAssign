# Optional: Add your API key below if you want to use live LLM APIs instead of the local deterministic engine.
# If both are empty, the pipeline runs 100% locally and offline.

GEMINI_API_KEY=""
OPENAI_API_KEY=""
